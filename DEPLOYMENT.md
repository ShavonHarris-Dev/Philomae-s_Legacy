/**
 * Development setup instructions
 * 
 * For local development:
 * 1. Copy firebase-config.template.js to firebase-config.js
 * 2. Update your API key in firebase-config.js
 * 3. Change index.html to use /firebase-config.js instead of /firebase-config.prod.js
 * 
 * For production:
 * - Uses firebase-config.prod.js (already configured)
 * - Safe to commit to version control
 * - No sensitive environment variables needed
 */